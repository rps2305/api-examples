# EAL ATS

## Overview
EAL ATS is an applicant tracking system (ATS) by EAL. Integrations typically use vendor-provided APIs or exports; consult EAL documentation for available endpoints.

## Python
```python
# Use the EAL ATS API endpoints provided in your tenant documentation.
# Example placeholder:
# requests.get("https://your-ats.eal.nl/api/v1/applicants", headers={"Authorization": "Bearer TOKEN"})
```

## PowerShell
```powershell
# Example placeholder:
# Invoke-RestMethod -Uri "https://your-ats.eal.nl/api/v1/applicants" -Headers @{ Authorization = "Bearer TOKEN" }
```

## curl
```bash
# curl -H "Authorization: Bearer TOKEN" https://your-ats.eal.nl/api/v1/applicants
```

## Docs
- https://www.eal.nl
